stages:
  - release

deploy_app:
  stage: release
  variables:
    PLAYBOOK_VARIABLES: "ssh_private_key=$SSH_PRIVATE_KEY"
  only:
    - master
  tags:
    - ansible
  script:
    - 'ansible-playbook ./ansible/deploy.yml -i ./ansible/hosts --extra-vars=$PLAYBOOK_VARIABLES'
